# Ejecutables compilados
*.exe
*.obj
*.o
*.ilk
*.pdb

# Directorios de build (CMake, Visual Studio, CLion, etc.)
build/
out/
.vs/
cmake-build-*/
.vscode/

# Archivos de datos generados en runtime (tablas, WAL, dumps)
# La fuente de verdad es data/raw/*.csv; lo demas se reconstruye al ejecutar.
data/storage/

# Configuracion local (puede contener claves/secrets)
opencode.json

# Notas y documentacion generadas
*.md

#Tablas  y wal innecesario subir a github
*.tbl
*.wal
*.bin
*.schema
*.idx
*.idx.meta